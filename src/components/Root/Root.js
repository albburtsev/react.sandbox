'use strict';

/**
 * Root component, once on page
 */
class Root extends React.Component{
    render() {
        return (
            <div className="root">Hello, React!</div>
        );
    }
}

module.exports = Root;
